import { escapeHtml as esc, evidenceLabel, filterRecords, paginate } from "./state.js";

const pages = [
  ["workflow","⌁","工作流"],["overview","◫","概览"],["search","⌕","搜索"],
  ["resources","◇","资料"],["workbench","▦","工作台"],["explain","?","解释"],
  ["research-map","◎","科研地图"],
  ["evidence-matrix","▤","证据矩阵"],["advanced","⋯","高级查看"],
];
const state = {page:"overview",snapshot:null,token:null,draftQuery:"",committedQuery:"",selectedKind:"all",searchPage:1,selectedResourceKind:null,resourcePage:1,eventPage:1,searchComposing:false,pendingSearchRender:false,glossaryOpen:new Set(),advancedOpen:new Set(),records:new Map()};
const $ = q => document.querySelector(q), bridge = () => window.pywebview?.api;
function notice(message,tone="info"){const n=$("#notice");n.textContent=message;n.className=`notice ${tone}`;clearTimeout(notice.timer);notice.timer=setTimeout(()=>n.classList.add("hidden"),6000);}
async function invoke(method,...args){const api=bridge();if(!api||typeof api[method]!=="function")throw new Error("本地桥接尚未就绪");return api[method](...args);}
function panel(title,body,kicker="LOCAL SNAPSHOT",extra=""){return `<article class="panel"><header><div><small>${kicker}</small><h2>${esc(title)}</h2></div>${extra}</header>${body}</article>`;}
function cards(values,clickable=false){return `<div class="metrics ${clickable?"resource-cards":""}">${values.map(x=>`<article ${clickable?`class="resource-card" role="button" tabindex="0" data-resource-path="${esc(x[4])}"`:""}><span>${esc(x[0])}</span><b class="${x[3]||""}">${esc(x[1])}</b><small>${esc(x[2])}</small></article>`).join("")}</div>`;}
function table(records,columns,kind=""){if(!records.length)return `<div class="empty">暂无记录</div>`;return `<div class="table-wrap"><table><thead><tr>${columns.map(x=>`<th>${esc(x[1])}</th>`).join("")}</tr></thead><tbody>${records.map((record,i)=>{const key=`${kind}:${record.event_id||record.item_id||record.task_id||i}`;state.records.set(key,record);return `<tr ${kind?`data-record-key="${esc(key)}" data-kind="${kind}" tabindex="0"`:""}>${columns.map(x=>`<td>${esc(record[x[0]])}</td>`).join("")}</tr>`;}).join("")}</tbody></table></div>`;}
function renderNavigation(){$("#navigation").innerHTML=pages.map(([id,icon,label])=>`<button data-page="${id}" class="${state.page===id?"active":""}"><span>${icon}</span>${label}</button>`).join("");$("#navigation").querySelectorAll("button").forEach(b=>b.onclick=()=>{state.page=b.dataset.page;state.graphFocus=null;renderNavigation();render();});}
function statusStrip(s){const lifecycle=s.action_matrix?.state||{},step=lifecycle.lifecycle_step||{},active=lifecycle.active_task||{};$("#status-strip").className="status-strip";$("#status-strip").innerHTML=`<span class="pulse"></span><b>${esc(step.label|| (active.task_id?"任务进行中":"当前无活动任务"))}</b><code>${esc(s.branch)}</code><span>STATE · ${esc((state.token||"").slice(0,8).toUpperCase())}</span>`;}

function overview(s){const c=s.context||{},h=s.health||{},a=s.attempt||{},p=c.project_profile||{},o=c.overview_state||c.state||{};return cards([["当前任务",c.active_task?"1":"0",c.active_task?.goal||"无活动任务"],["资料索引",(s.catalog_items||[]).length,`${(s.catalog_relations||[]).length} 条显式关系`,"accent"],["探索尝试",(s.explorations||[]).length+(a.attempt_id?1:0),a.state||"无当前尝试"],["系统健康",h.status||"未知",`${h.events||0} events`,h.status==="passed"?"good":"warn"]])+`<div class="grid two">${panel("项目主线",`<dl class="details"><dt>描述</dt><dd>${esc(p.description)}</dd><dt>大目标</dt><dd>${esc(p.big_goal)}</dd><dt>当前判断</dt><dd>${esc(o.judgment)}</dd><dt>断点</dt><dd>${esc(o.breakpoint)}</dd></dl>`,"PROJECT STATE")}${panel("下一步",`<ol class="steps">${(c.visible_next_steps||o.next_steps||[]).map(x=>`<li>${esc(x)}</li>`).join("")||"<li>暂无登记的下一步</li>"}</ol>`,"NEXT ACTION")}</div>`;}
function workflow(s){const l=s.action_matrix?.state||{},active=l.active_task||{},writer=l.writer_lock||{},step=l.lifecycle_step||{},git=l.git||{},attempt=l.attempt||s.attempt||{},labels=["未开始","周期已建立","工作中","进展已记录","可完成","正在收尾","已完成"];const steps=labels.map((x,i)=>`<span class="${i===step.index?"current":""}">${x}</span>`).join("<i>→</i>");const detail=active.task_id?`<dl class="details"><dt>任务</dt><dd>${esc(active.task_id)}</dd><dt>轨道</dt><dd>${esc(active.track||attempt.track||"stable")}</dd><dt>开始</dt><dd>${esc(active.started_at)}</dd><dt>变化</dt><dd>${(l.changed_paths||[]).length} 个文件</dd><dt>进度</dt><dd>${active.state_updated?"已记录":"未记录"}</dd><dt>决策</dt><dd>${active.decisions_added||0}</dd><dt>写锁</dt><dd>${writer.status==="active"?`${esc(writer.command)} / PID ${esc(writer.pid)}`:"空闲"}</dd><dt>工作树稳定</dt><dd>${esc(l.worktree_quiet_seconds||0)} 秒</dd></dl>`:`<p class="muted">当前没有活动工作周期。最近完成：${esc(l.last_completed?.task_id||"无")}</p>`;const delivery=`<dl class="details"><dt>工作区</dt><dd>${(l.dirty_paths||[]).length?`${l.dirty_paths.length} 个未提交变化`:"干净"}</dd><dt>提交/推送</dt><dd>${esc(git.relation||"未知")}</dd><dt>恢复</dt><dd>${esc(l.sidecar?.error||"无需恢复")}</dd></dl>`;return panel("实时生命周期",`<div class="lifecycle-steps">${steps}</div>${detail}`,"LIVE · 3 SECOND REFRESH")+`<div class="grid two">${panel("交付状态",delivery,"WORKTREE · COMMIT · PUSH")}${panel("安全边界",`<p class="muted">Web 工作台保持业务只读。任务、阶段、探索、决策和资料关系仍由结构化 CLI 生命周期写入。</p><div class="boundary"><b>允许</b><span>刷新 · 更新检查 · 诊断导出 · 打开受限资料 · 复制上下文</span><b>禁止</b><span>任意命令 · 任意文件读取 · SQLite 写入</span></div>`,"READ ONLY")}</div>${panel("最近完成",table((s.history||[]).slice(0,10),[["occurred_at","时间"],["summary","任务"],["result","结果"],["branch","分支"]],"history"),"TASK HISTORY")}`;}
function search(s){const source=s.search_index||[],types=[["all","全部"],["task","任务"],["exploration","探索"],["decision","决策"],["catalog","资料"]],counts=Object.fromEntries(types.map(([k])=>[k,k==="all"?source.length:source.filter(x=>x.kind===k).length])),typed=state.selectedKind==="all"?source:source.filter(x=>x.kind===state.selectedKind),hasCriteria=Boolean(state.committedQuery)||state.selectedKind!=="all",filtered=hasCriteria?filterRecords(typed,state.committedQuery,["title","summary","search_text","branch","kind_label"]):[],page=paginate(filtered,state.searchPage,10);state.searchPage=page.page;return panel("跨记录搜索",`<div class="type-chips" role="group" aria-label="记录类型">${types.map(([k,label])=>`<button data-search-kind="${k}" class="${state.selectedKind===k?"active":""}" aria-pressed="${state.selectedKind===k}">${label}<span>${counts[k]}</span></button>`).join("")}</div><form id="search-form" class="search-controls"><label class="searchbox"><span>⌕</span><input id="search-input" value="${esc(state.draftQuery)}" placeholder="搜索任务、探索、决策、资料…"></label><button class="primary-button" type="submit">搜索</button><button id="search-clear" type="button">清除</button></form>${hasCriteria?`<p class="count">${state.committedQuery?`“${esc(state.committedQuery)}” · `:""}${filtered.length} 条结果 · 第 ${page.page}/${page.pages} 页</p>${table(page.items,[["kind_label","类型"],["title","标题"],["summary","摘要"],["branch","分支"]],"search")}<div class="pagination"><button id="search-prev" ${page.page<=1?"disabled":""}>上一页</button><button id="search-next" ${page.page>=page.pages?"disabled":""}>下一页</button></div>`:`<div class="empty">输入关键词，或点击类型快速浏览</div>`}`,"INDEX · LOCAL");}
function resources(s){
  const dirs=s.resource_directories||[],selected=dirs.find(x=>x.kind===state.selectedResourceKind),items=selected?(s.catalog_items||[]).filter(x=>x.kind===selected.kind):[],page=paginate(items,state.resourcePage,10);state.resourcePage=page.page;
  const directoryCards=`<div class="resource-grid">${dirs.map(x=>`<article class="resource-directory ${state.selectedResourceKind===x.kind?"active":""}" role="button" tabindex="0" data-resource-kind="${esc(x.kind)}"><header><div><small>${esc(x.path)}</small><h3>${esc(x.label)}</h3></div><span class="badge ${x.status==="ok"?"good":"warn"}">${esc(x.status)}</span></header><p>${esc(x.description)}</p><div class="resource-counts"><b>${x.actual_files}</b> 个文件 · <b>${x.indexed_files}</b> 已登记</div><button data-open-resource="${esc(x.path)}">打开文件夹</button></article>`).join("")}</div>`;
  const actions=`<button id="search-catalog" class="primary-button">搜索全部资料</button>`;
  const content=selected?`<p class="count">${esc(selected.label)} · ${items.length} 条登记资料 · 第 ${page.page}/${page.pages} 页</p>${table(page.items,[["kind_label","类型"],["title","标题"],["status","状态"],["path","项目路径"],["branch","分支"]],"catalog")}<div class="pagination"><button id="resource-prev" ${page.page<=1?"disabled":""}>上一页</button><button id="resource-next" ${page.page>=page.pages?"disabled":""}>下一页</button></div>`:`<div class="empty">选择一个标准目录浏览登记资料；使用卡片内按钮可直接打开文件夹。</div>`;
  return panel("标准资料目录",directoryCards,"BROWSE · OPEN FOLDER",actions)+panel(selected?`${selected.label}资料`:"目录资料",content,"CATALOG · CLICK TO REVEAL");
}
function workbench(s){return `<div class="grid two">${panel("科研上下文",`<p class="muted">复制已登记资料的 Markdown 上下文，不会自动上传内容。</p><button id="copy-context" class="primary-button">复制全部登记资料上下文</button>`,"CONTEXT")}${panel("外置软件",table(s.external_tools||[],[["kind_label","类型"],["name","名称"],["status","状态"]]),"REGISTERED TOOLS")}</div>`;}
function explain(s){return panel("中英文状态解释",`<p class="muted">展开类别并选择英文状态，查看中文含义、实际影响和安全下一步。</p><div class="glossary">${(s.status_glossary||[]).map(group=>`<details data-glossary-section="${esc(group.section)}" ${state.glossaryOpen.has(group.section)?"open":""}><summary>${esc(group.section)} <span>${group.items.length}</span></summary>${group.items.map(item=>`<button class="glossary-item" data-glossary="${esc(group.section)}:${esc(item.code)}"><b>${esc(item.code)}</b><span>${esc(item.meaning)}</span></button>`).join("")}</details>`).join("")}</div>`,"STATUS GLOSSARY");}
function graphView(s){
  const t=s.research?.timeline||{versions:[],explorations:[],files:[],edges:[],unassigned:{}},width=Math.max(1200,t.versions.length*270+180),positions=new Map();
  t.versions.forEach((v,i)=>positions.set(v.id,{...v,x:120+i*270,y:310,nodeKind:"version",label:v.version}));
  const byVersion=new Map(t.versions.map((v,i)=>[v.version,{index:i,up:0,down:0}]));
  t.explorations.forEach(x=>{const slot=byVersion.get(x.version);positions.set(x.id,{...x,x:120+slot.index*270+(slot.up++%2)*65-30,y:105-Math.floor((slot.up-1)/2)*48,nodeKind:"exploration"})});
  t.files.forEach(x=>{const slot=byVersion.get(x.version);positions.set(x.id,{...x,x:120+slot.index*270+(slot.down++%2)*65-30,y:555+Math.floor((slot.down-1)/2)*48,nodeKind:x.kind||"file"})});
  const nodes=[...positions.values()].map(n=>n.nodeKind==="version"?`<g class="timeline-node version" data-timeline-node="${esc(n.id)}" transform="translate(${n.x} ${n.y})" tabindex="0" role="button"><foreignObject x="-115" y="-80" width="230" height="160"><div xmlns="http://www.w3.org/1999/xhtml" class="timeline-version-card"><header><b>${esc(n.version)}</b><span>${n.task_count}任务 · ${n.exploration_count}探索</span></header><small>建立目标</small><p>${esc(n.initial_goal||"未登记")}</p><small>最终判断</small><p>${esc(n.final_judgment||"未登记")}</p></div></foreignObject></g>`:`<g class="timeline-node ${esc(n.nodeKind)}" data-timeline-node="${esc(n.id)}" transform="translate(${n.x} ${n.y})" tabindex="0" role="button"><rect x="-55" y="-24" width="110" height="48" rx="9"></rect><text text-anchor="middle" y="4">${esc(n.label||n.version).slice(0,16)}</text></g>`).join("");
  return panel("主目标版本线索",`${!t.files.length?`<div class="notice warn">使用 core/map 标签或 catalog 关系将关键文件加入地图。</div>`:""}<div class="timeline-stats">${t.versions.length} 个版本 · ${t.explorations.length} 次探索 · ${t.files.length} 个关键文件线索</div><div class="timeline-canvas"><svg viewBox="0 0 ${width} 680" style="min-width:${width}px">${t.edges.map(e=>{const a=positions.get(e.source),b=positions.get(e.target);return a&&b?`<line x1="${a.x}" y1="${a.y}" x2="${b.x}" y2="${b.y}" class="${e.kind}"/>`:""}).join("")}${nodes}</svg></div>${(t.unassigned?.explorations||[]).length?`<p class="muted">${t.unassigned.explorations.length} 条探索暂未归属版本，已保留在详情数据中。</p>`:""}`,"VERSION · EXPLORATION · KEY FILES");
}
function matrix(s){const d=s.research?.evidence_matrix||{rows:[]};return panel("证据矩阵",`<p class="muted">只展示已登记的 supports / validates / contradicts；“未登记”不等于“没有证据”。</p><div class="table-wrap"><table class="matrix"><thead><tr><th>理论</th><th>论文</th><th>实验 / 仿真</th><th>结果</th></tr></thead><tbody>${d.rows.map(r=>`<tr><th>${esc(r.theory.title)}</th>${["paper","experiment","result"].map(k=>`<td class="${r.cells[k].status}">${esc(evidenceLabel(r.cells[k]))}</td>`).join("")}</tr>`).join("")||`<tr><td colspan="4" class="empty">暂无已登记理论</td></tr>`}</tbody></table></div>`,"EXPLICIT EVIDENCE ONLY");}
function disclosure(id,title,count){return `<details class="panel disclosure" data-lazy="${id}" ${state.advancedOpen.has(id)?"open":""}><summary><span><small>ADVANCED</small><b>${esc(title)}</b></span><em>${esc(count)}</em></summary><div class="lazy-body empty">展开后读取</div></details>`;}
function advanced(s){const issues=s.diagnostics?.issues||s.diagnostics?.active||[],actions=s.action_matrix?.actions||[];return disclosure("runtime","版本与运行状态","按需")+disclosure("actions","动作可用性",`${actions.length} 项`)+disclosure("diagnostics","诊断",`${issues.length} 个问题`)+disclosure("events","原始事件",`${(s.events||[]).length} 条`);}
function fillDisclosure(node,s,force=false){const id=node.dataset.lazy,body=node.querySelector(".lazy-body");if(body.dataset.loaded&&!force)return;body.dataset.loaded="1";body.classList.remove("empty");if(id==="runtime")body.innerHTML=`<div class="button-row"><button id="check-updates" class="primary-button">检查更新</button><button id="software-refresh">刷新软件交付状态</button></div><pre>${esc(JSON.stringify({branch:s.branch,health:s.health,classification:s.classification,active_task_warning:s.active_task_warning},null,2))}</pre>`;if(id==="actions")body.innerHTML=table(s.action_matrix?.actions||[],[["category","类别"],["label","动作"],["status","状态"],["missing_fields","等待输入"]]);if(id==="diagnostics"){const d=s.diagnostics||{},issues=d.issues||d.active||[];body.innerHTML=`<div class="button-row"><button id="export-diagnostics" class="primary-button">导出脱敏 ZIP</button><button id="report-bug">报告 Bug</button></div>${table(issues,[["occurred_at","时间"],["code","代码"],["summary","摘要"]],"diagnostic")}`;}if(id==="events"){const page=paginate([...(s.events||[])].reverse(),state.eventPage,50);state.eventPage=page.page;body.innerHTML=`<p class="count">第 ${page.page}/${page.pages} 页</p>${table(page.items,[["occurred_at","时间"],["event_type","类型"],["task_id","任务"],["branch","分支"]],"event")}<div class="pagination"><button id="event-prev" ${page.page<=1?"disabled":""}>上一页</button><button id="event-next" ${page.page>=page.pages?"disabled":""}>下一页</button></div>`;}bindDynamic(s);}
const advancedDependencies={runtime:new Set(["branch","health","classification","active_task_warning","software_delivery"]),actions:new Set(["action_matrix"]),diagnostics:new Set(["diagnostics"]),events:new Set(["events"])};
function updateAdvanced(s,changed=[],force=false){const issues=s.diagnostics?.issues||s.diagnostics?.active||[],counts={runtime:"按需",actions:`${(s.action_matrix?.actions||[]).length} 项`,diagnostics:`${issues.length} 个问题`,events:`${(s.events||[]).length} 条`},scrollY=window.scrollY;document.querySelectorAll("details[data-lazy]").forEach(node=>{node.querySelector("summary em").textContent=counts[node.dataset.lazy];const affected=force||changed.some(key=>advancedDependencies[node.dataset.lazy]?.has(key));if(node.open&&affected)fillDisclosure(node,s,true);});requestAnimationFrame(()=>window.scrollTo(0,scrollY));}
function render(){state.records.clear();const s=state.snapshot,p=pages.find(x=>x[0]===state.page);$("#page-title").textContent=p?.[2]||"概览";if(!s){$("#content").innerHTML=`<div class="loading-card"><span></span>正在准备本地只读快照…</div>`;return;}const views={workflow,overview,search,resources,workbench,explain,"research-map":graphView,"evidence-matrix":matrix,advanced};$("#content").innerHTML=views[state.page](s);bindDynamic(s);}
function openDrawer(title,value){$("#drawer-body").innerHTML=`<small>DETAIL</small><h2 id="drawer-title">${esc(title)}</h2><pre>${esc(JSON.stringify(value,null,2))}</pre>`;$("#drawer").classList.remove("hidden");}
function bindDynamic(s){
  const input=$("#search-input");
  input?.addEventListener("compositionstart",()=>state.searchComposing=true);
  input?.addEventListener("compositionend",e=>{state.searchComposing=false;state.draftQuery=e.target.value;if(state.pendingSearchRender){state.pendingSearchRender=false;render();}});
  input?.addEventListener("input",e=>state.draftQuery=e.target.value);
  input?.addEventListener("blur",()=>{if(state.pendingSearchRender&&!state.searchComposing){state.pendingSearchRender=false;render();}});
  $("#search-form")?.addEventListener("submit",e=>{e.preventDefault();state.draftQuery=input?.value||state.draftQuery;state.committedQuery=state.draftQuery.trim();state.searchPage=1;state.pendingSearchRender=false;render();});
  $("#search-clear")?.addEventListener("click",()=>{state.draftQuery="";state.committedQuery="";state.selectedKind="all";state.searchPage=1;render();});
  document.querySelectorAll("[data-search-kind]").forEach(n=>n.onclick=()=>{state.selectedKind=n.dataset.searchKind;state.searchPage=1;render();});
  $("#search-prev")?.addEventListener("click",()=>{state.searchPage--;render();});$("#search-next")?.addEventListener("click",()=>{state.searchPage++;render();});
  document.querySelectorAll("[data-resource-kind]").forEach(n=>{const select=()=>{state.selectedResourceKind=n.dataset.resourceKind;state.resourcePage=1;render()};n.onclick=select;n.onkeydown=e=>{if(e.target===n&&(e.key==="Enter"||e.key===" ")){e.preventDefault();select();}}});
  document.querySelectorAll("[data-open-resource]").forEach(n=>n.onclick=async e=>{e.stopPropagation();const r=await invoke("open_resource_directory",n.dataset.openResource);notice(r.ok?`已打开 ${r.data.path}`:r.error.message,r.ok?"info":"error")});
  $("#search-catalog")?.addEventListener("click",()=>{state.page="search";state.selectedKind="catalog";state.draftQuery="";state.committedQuery="";state.searchPage=1;renderNavigation();render();});
  $("#resource-prev")?.addEventListener("click",()=>{state.resourcePage--;render();});$("#resource-next")?.addEventListener("click",()=>{state.resourcePage++;render();});
  document.querySelectorAll("tr[data-record-key]").forEach(row=>{const open=async()=>{const record=state.records.get(row.dataset.recordKey);if(row.dataset.kind==="catalog"){const r=await invoke("reveal_resource_file",record.item_id);notice(r.ok?`已定位 ${r.data.path}`:r.error.message,r.ok?"info":"error");}else openDrawer("记录详情",record)};row.onclick=open;row.onkeydown=e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();open();}}});
  document.querySelectorAll("details[data-glossary-section]").forEach(n=>n.addEventListener("toggle",()=>{if(n.open)state.glossaryOpen.add(n.dataset.glossarySection);else state.glossaryOpen.delete(n.dataset.glossarySection);}));
  document.querySelectorAll(".glossary-item").forEach(btn=>btn.onclick=()=>{const [section,code]=btn.dataset.glossary.split(":");const item=(s.status_glossary||[]).find(x=>x.section===section)?.items.find(x=>x.code===code);openDrawer(`${code}｜${item.meaning}`,{类别:section,英文状态:code,中文含义:item.meaning,意味着什么:item.implication,安全下一步:item.next_step});});
  document.querySelectorAll("[data-timeline-node]").forEach(n=>{const open=()=>{const t=s.research?.timeline||{},id=n.dataset.timelineNode,record=[...(t.versions||[]),...(t.explorations||[]),...(t.files||[])].find(x=>x.id===id);openDrawer("版本线索详情",record)};n.onclick=open;n.onkeydown=e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();open();}}});
  document.querySelectorAll("details[data-lazy]").forEach(n=>{if(!n.dataset.bound){n.dataset.bound="1";n.addEventListener("toggle",()=>{if(n.open){state.advancedOpen.add(n.dataset.lazy);fillDisclosure(n,state.snapshot);}else state.advancedOpen.delete(n.dataset.lazy);});}if(n.open)fillDisclosure(n,s);});
  $("#copy-context")?.addEventListener("click",async()=>{const r=await invoke("copy_context",null);if(r.ok){await navigator.clipboard.writeText(r.data.text);notice("AI 上下文已复制");}else notice(r.error.message,"error")});$("#export-diagnostics")?.addEventListener("click",async()=>{const r=await invoke("export_diagnostics",null);notice(r.ok?`诊断包已保存：${r.data.output}`:r.error.message,r.ok?"info":"error")});$("#report-bug")?.addEventListener("click",async()=>{const r=await invoke("report_bug");notice(r.ok&&r.data.opened?"已打开 Bug 报告页面":"无法打开 Bug 报告页面",r.ok?"info":"error")});$("#check-updates")?.addEventListener("click",async()=>openDrawer("更新检查",await invoke("check_updates")));$("#software-refresh")?.addEventListener("click",async()=>openDrawer("软件交付状态",await invoke("refresh_software_delivery",{})));$("#event-prev")?.addEventListener("click",()=>{state.eventPage--;const n=document.querySelector('[data-lazy="events"]');n.querySelector(".lazy-body").dataset.loaded="";fillDisclosure(n,s)});$("#event-next")?.addEventListener("click",()=>{state.eventPage++;const n=document.querySelector('[data-lazy="events"]');n.querySelector(".lazy-body").dataset.loaded="";fillDisclosure(n,s)});
}
const pageSections={workflow:new Set(["action_matrix","context","history","attempt"]),overview:new Set(["context","health","attempt","catalog_items","catalog_relations"]),search:new Set(["search_index"]),resources:new Set(["resource_directories","catalog_items"]),explain:new Set(["status_glossary"]),"research-map":new Set(["research"]),advanced:new Set(["branch","health","classification","active_task_warning","software_delivery","action_matrix","diagnostics","events"])};
async function refresh(force=false){$("#refresh-button").disabled=true;try{const r=await invoke("refresh",state.token,force),changed=r.data?.changed_sections||[],hasSnapshot=Boolean(r.data?.snapshot);if(hasSnapshot)state.snapshot=r.data.snapshot;if(r.data?.state_token)state.token=r.data.state_token;if(!r.ok)notice(`刷新失败，继续显示最后快照：${r.error.message}`,"error");statusStrip(state.snapshot);const relevant=force||(hasSnapshot&&changed.some(x=>(pageSections[state.page]||new Set()).has(x)));if(state.page==="advanced"&&relevant)updateAdvanced(state.snapshot,changed,force);else if(relevant&&state.page==="search"&&(state.searchComposing||document.activeElement?.id==="search-input")){state.pendingSearchRender=true;}else if(relevant)render();}catch(e){notice(e.message,"error")}finally{$("#refresh-button").disabled=false;}}
function initialize(){const requested=new URLSearchParams(location.hash.slice(1)).get("theme"),preferred=matchMedia("(prefers-color-scheme: light)").matches?"light":"dark";$("#app").dataset.theme=["light","dark"].includes(requested)?requested:preferred;renderNavigation();render();$("#refresh-button").onclick=()=>refresh(true);$("#theme-toggle").onclick=()=>{const a=$("#app");a.dataset.theme=a.dataset.theme==="dark"?"light":"dark"};$("#drawer-close").onclick=()=>$("#drawer").classList.add("hidden");document.addEventListener("keydown",e=>{if(e.key==="Escape")$("#drawer").classList.add("hidden")});window.addEventListener("pywebviewready",async()=>{const ready=await invoke("ready");$("#runtime-version").textContent=`v${ready.data.version} · bridge ready`;await refresh(true);if(ready.data.refresh_seconds>0)setInterval(()=>refresh(false),ready.data.refresh_seconds*1000);});}
initialize();
